"""
OCRKit — A lightweight OCR + UI structure extraction toolkit.

Converts screenshots into structured JSON describing UI elements
(labels, buttons, text blocks, input fields, etc.) with positions.
"""

__version__ = "0.1.0"

from ocrkit.pipeline import OCRKitPipeline  # noqa: F401
